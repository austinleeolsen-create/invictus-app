import{redirect}from"next/navigation";import{createSupabaseServerClient}from"@/lib/supabase/server";import{updatePassword}from"./actions";
export default async function UpdatePasswordPage({searchParams}:{searchParams:Promise<{error?:string}>}){const supabase=await createSupabaseServerClient(),{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login?error=Open the newest password reset email first.");const{error}=await searchParams;return <main className="login-shell"><section className="login-card"><div className="brand-mark">I</div><p className="eyebrow">Invictus Basketball Club</p><h1>Choose a new password</h1><p className="muted">Use at least 10 characters and do not reuse a temporary password.</p><form action={updatePassword} className="login-form"><label>New password<input name="password" type="password" autoComplete="new-password" minLength={10} required/></label><label>Confirm new password<input name="confirm" type="password" autoComplete="new-password" minLength={10} required/></label>{error?<p className="error">{error}</p>:null}<button>Save new password</button></form></section></main>}
