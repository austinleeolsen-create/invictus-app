import{NextResponse}from"next/server";import{createSupabaseServerClient}from"@/lib/supabase/server";
export async function GET(){const supabase=await createSupabaseServerClient(),{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.redirect(new URL("/login",process.env.NEXT_PUBLIC_APP_URL??"https://invictus-app-kappa.vercel.app"));const{data:profile}=await supabase.from("profiles").select("role").eq("id",user.id).single();if(!["owner_admin","program_director"].includes(profile?.role??""))return NextResponse.json({error:"Operations manager access required."},{status:403});const clientId=process.env.GROUPME_CLIENT_ID;if(!clientId)return NextResponse.json({error:"GROUPME_CLIENT_ID is not configured."},{status:500});return NextResponse.redirect(`https://oauth.groupme.com/oauth/authorize?client_id=${encodeURIComponent(clientId)}`)}
