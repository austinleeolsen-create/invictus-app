import Link from"next/link";import{requestPasswordReset}from"./actions";
export default async function ForgotPasswordPage({searchParams}:{searchParams:Promise<{sent?:string;error?:string}>}){const{sent,error}=await searchParams;return <main className="login-shell"><section className="login-card"><div className="brand-mark">I</div><p className="eyebrow">Invictus Basketball Club</p><h1>Reset password</h1>{sent?<><p className="reset-success">Check your email for a secure password-reset link.</p><p className="muted">The link can only be used once. If it expires, request another one here.</p></>:<><p className="muted">Enter the email address used for your Invictus Hub login.</p><form action={requestPasswordReset} className="login-form"><label>Email<input name="email" type="email" autoComplete="email" required/></label>{error?<p className="error">{error}</p>:null}<button>Send reset email</button></form></>}<Link className="forgot-password-link" href="/login">Back to sign in</Link></section></main>}
