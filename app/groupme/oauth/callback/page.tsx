"use client";import{useEffect,useState}from"react";
export default function GroupMeCallback(){const[message,setMessage]=useState("Connecting GroupMe…");useEffect(()=>{const params=new URLSearchParams(window.location.search||window.location.hash.slice(1)),token=params.get("access_token");window.history.replaceState({},"",window.location.pathname);if(!token){setMessage("GroupMe did not return access. Return to the Hub and try again.");return}fetch("/api/groupme/oauth/store",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({accessToken:token})}).then(async response=>{const body=await response.json();if(!response.ok)throw new Error(body.error);window.location.href="/?view=groupme"}).catch(error=>setMessage(error.message??"Unable to connect GroupMe."))},[]);return <main className="login-page"><section className="login-card"><p className="eyebrow">GroupMe setup</p><h1>{message}</h1><p className="muted">You can close this page if it does not continue automatically.</p></section></main>}
